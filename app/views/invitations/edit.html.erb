 <%= render "shared/navbar" %>
<h1>Invitations</h1>
<% if @event.attendees.include?(current_user)%>
  <%= button_to "Leave Event", invitation_path, method: :delete %>
<% else %>
  <%= button_to "Join Event", invitation_path, method: :put %>
<% end %>
